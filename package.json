{
  "name": "agency-superpowers-product-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm --prefix frontend run build",
    "start:frontend": "npm --prefix frontend run dev -- -p 3000",
    "start:backend": "cd backend && python -m uvicorn api.main:app --host 0.0.0.0 --port 8000",
    "start:backend-node": "npm --prefix backend-node run start"
  }
}
